package de.qwikbyte.datacore.orm;

/**
 * Represents a generic repository interface for managing entities of type {@code T}.
 *
 * This interface is intended to serve as a base for implementing repository patterns
 * to handle data access and operations for entities within the application. It provides
 * an abstraction layer that can be extended and customized as needed by specific
 * repositories for different entity types.
 *
 * @param <T> the type of the entity managed by the repository.
 */
public interface Repository<T> {
}
